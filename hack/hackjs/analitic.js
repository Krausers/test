var hack = false;
var number = getRandomInRange(10, 99);
var access = getRandomInRange(10, 99);

if (access != number) {
    hack = true;
    var number_false = access;
    var number_true = number;
}